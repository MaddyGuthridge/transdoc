# Transdoc Plaintext Handler

Out-of-the-box, Transdoc can transform plaintext files such
as `.txt` and `.md`.

All content from these files is transformed when matching
`{{` and `}}` pairs are found.
